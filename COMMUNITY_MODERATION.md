# NEBULA 社区内容审核机制文档

> 版本：v1.0（2026-08）｜适用范围：NEBULA 学习社区（课程分享、评论）与学校申请
> 原则：**机器初审 + 人工复核** 双防线，兼顾安全与发布体验。

## 1. 总体流程

```
用户提交（帖子/评论/学校申请）
        │
        ▼
┌───────────────────┐    通过     ┌──────────────┐
│ ① AI 自动审核      │ ──────────▶ │ 立即公开可见   │
│ （DeepSeek 审核员）│             └──────────────┘
└─────────┬─────────┘
          │ 不通过 / AI 服务异常
          ▼
┌───────────────────┐   通过     ┌──────────────┐
│ ② 管理员人工复核    │ ──────────▶ │ 公开可见      │
│ （后台 #/admin）    │            └──────────────┘
└─────────┬─────────┘
          │ 驳回
          ▼
    内容隐藏（用户可见"未通过"状态）
```

## 2. AI 自动审核（第一道防线）

### 2.1 审核对象与判定标准

AI 审核员按以下维度判定（输出 JSON：`{approved, reason, risk}`）：

| 维度 | 判定为不通过的情形 |
|---|---|
| 广告营销 | 引流微信、卖课、推广链接、垃圾 SEO 内容 |
| 垃圾信息 | 无意义刷屏、重复内容、乱码 |
| 人身攻击 | 辱骂、骚扰、歧视、人身威胁 |
| 不当内容 | 色情低俗、暴力血腥、政治敏感 |
| 无关内容 | 与学习/教育完全无关的灌水 |

风险分级：`low`（无风险）/ `medium`（边界内容）/ `high`（明确违规）。

### 2.2 审核范围

| 内容类型 | AI 审核 | 不通过后 |
|---|---|---|
| 社区课程帖子（标题+描述+课程摘要） | ✅ 强制 | **进入人工复核队列** |
| 评论 | ✅ 强制 | 直接隐藏（不入队列，管理员可在后台删除） |
| 学校申请 | 名称规范检查（非 AI） | 进入后台学校审核页签 |

### 2.3 技术实现

- 模型：DeepSeek（deepseek-v4-flash），`temperature 0.1`，JSON 输出 + 容错解析
- 异常兜底：AI 服务不可用时**不直接放行**——帖子转人工复核队列（`status=pending`，审核意见标注"AI 审核服务暂不可用"），评论默认放行（低风险场景，管理员可事后删除）
- MOCK 模式：含"垃圾/广告/test-reject"字样即拒绝，便于联调

## 3. 管理员人工复核（第二道防线）

### 3.1 入口

网站侧边栏 → 🛡️ 管理后台（`#/admin`）→ 「人工复核队列」页签。
管理员身份：注册邮箱在 Worker 环境变量 `ADMIN_EMAILS`（逗号分隔）中的账号。

### 3.2 队列信息

每条待复核项展示：标题、主题、作者、提交时间、**AI 审核意见与原因**、风险等级、描述、课程内容预览（内嵌 iframe 试玩）。

### 3.3 操作

- **通过**：帖子立即公开，`human_reviewed=1`
- **驳回**：帖子状态 `rejected`，不公开；作者在"我的帖子"中可见"未通过"状态
- 学校页签：通过 = 加入全校学校下拉库；驳回 = 删除申请

## 4. 状态机

```
pending(待人工) ──管理员通过──▶ approved(公开)
       │
       └──管理员驳回──▶ rejected(不公开)
```

社区列表只展示 `approved`；作者本人可查看自己 `pending/rejected` 的帖子状态。

## 5. 安全与防滥用

- 发帖/评论/点赞/克隆均需登录（JWT 会话）
- 登录与注册经 Cloudflare Turnstile 人机验证
- 全接口限流（登录 20 次/分、注册 10 次/分、生成 60 次/分、工单 5 次/小时）
- 评论 AI 拒绝后隐藏；管理员「全部帖子」页签可删除任何违规内容
- 社区帖子引用的课程不可被作者删除（防止帖子悬空）

## 6. 相关接口

| 接口 | 说明 |
|---|---|
| `POST /api/community` | 发布 → AI 审核 → 返回 `{status: approved\|pending, review}` |
| `GET /api/community` | 列表（仅 approved，支持搜索/排序） |
| `GET /api/admin/moderation` | 人工复核队列 |
| `POST /api/admin/moderation/:id` | `{action: approve\|reject}` |
| `GET /api/admin/posts` / `DELETE /api/admin/posts/:id` | 帖子管理 |
| `GET /api/admin/schools` / `POST /api/admin/schools/:id` | 学校审核 |
