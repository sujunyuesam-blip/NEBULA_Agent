{
  "name": "nebula-worker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "NEBULA API Worker - AI generation pipeline, auth, D1 storage",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute nebula-db --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
