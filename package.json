{
  "name": "nebula-ai-agent",
  "version": "2.0.0",
  "private": true,
  "description": "NEBULA AI Agent - personalized educational agent incubator (V2: dynamic web + structured generation)",
  "workspaces": [
    "web",
    "worker"
  ],
  "scripts": {
    "dev:worker": "npm run dev --workspace worker",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace web",
    "test:api": "node scripts/smoke-api.mjs",
    "preview": "npm run preview --workspace web"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
