# NEBULA AI Agent — 产品介绍文档 & PRD

> 版本：v1.1（基于线上 v15.1.0-Fix 源码分析，按评审意见修订）
> 分析对象：https://www.nebulavessel.com （前端）+ https://api.nebulavessel.com （Cloudflare Worker 后端）
> 作者：Sam Su / DeepSeek R1 / Gemini 3 Pro（原项目署名）

---

## 1. 项目定位

**一句话定位：**
NEBULA AI Agent（全称 *Nebula AI Incubator Enterprise Edition*）是一个**「个性化教育 Agent 一键孵化器」**——用户只需要输入一个学习主题、一个身份角色和一个难度阶梯，系统就调用大模型实时"孵化"出一个为其**量身定制**的、游戏化的、可直接运行的交互式 HTML 学习应用。

**核心价值主张：**
在「枯燥的课本」与「漫无目的的 AI 搜索」之间架起桥梁，并且让这道桥**只为你而建**。课本单向灌输、AI 问答零散浅层；NEBULA 则把知识**编译成一段有剧情、有章节、有测验、有引导、有反馈的主动探索之旅**，让用户从"被动消费内容"变成"主动建构认知"，并且**零代码**获得一个可导出、可独立运行、**贴合自身身份与目标**的个性化学习组件。

**Slogan（现有设计语言）：**
- "Pixels with Purpose"（设计系统）
- "The ultimate educational agent generator powered by DeepSeek MoE"（官方描述）
- V2 建议补充中文口号：**「同一主题，千人千课」**

---

## 2. 我对项目的理解

### 2.1 它本质上是什么

NEBULA 不是传统问答机器人，而是一个 **「个性化 AI 生成式教育系统」**。它把"个性化"作为第一性原理：**同一个学习主题，输入不同的身份、目标、难度和语言，就会孵化出完全不同的课程**。

以"拉格朗日乘子法"为例：

| 用户身份 | 同一主题孵化的课程 |
|---|---|
| 外贸出口商 | 以"运输成本最小化、利润约束"为情境，聚焦实用计算与避坑 |
| 考研学生 | 严谨推导 + 题型训练 + 考场技巧 |
| 产品经理 | 类比化解释 + "何时该用它做决策"的直觉训练 |
| 法语学习者 | 法语界面 + 法语内容，母语化建构专业知识 |

### 2.2 产品理念（V2 重点梳理）

NEBULA 的理念可以概括为 **「四化」**：

**① 学习个性化（Personalization）—— 产品的灵魂**
- **身份驱动**：内容侧重点、案例、语言风格全部由用户身份与目标决定（外贸商得到获客实操，学生得到理论体系）。
- **难度自适应**：入门级用科普/趣味/类比，专家级含公式推导、建模与争议研究。
- **语言个性化**：8 种界面语言 + 任意目标语言生成内容，学习者用母语/目标语言深度探索知识。
- **反馈闭环个性化**：学习者提交感悟 → AI 评估理解深度 → 输出针对性改进建议 → 沉淀为学习画像，反哺下一次生成（V2 实现）。

**② 学习游戏化（Gamification）—— 体验引擎**
- 剧情引导、章节解锁、即时得分、音效反馈；把微观经济学的激励设计（即时回报、进度可见）内化为学习动力。

**③ 学习建构化（Constructivism）—— 教育内核**
- 强制引导式学习（Scaffolding）：答错必须弹窗解释"为什么错 + 如何重新思考"，答对才可前进——把测验从"考核"变成"认知支架"，用户主动建构而非被动接收。

**④ 生产零代码化（Zero-Code）—— 交付承诺**
- 用户不写一行代码，得到完整可运行的 HTML 学习应用，可预览、导出、离线分发。

### 2.3 产品体系：从「四层闭环」升级为「六层系统」

现有产品已形成四层闭环，V2 将丰富为**六层系统**，并新增贯穿全流程的质量评估体系：

| 层 | 名称 | 现有能力 | V2 增强 |
|---|---|---|---|
| L1 | **意图理解层** | 主题输入 + AI 领域识别 | 身份/目标深度解析（学习者建模）、主题可行性分析 |
| L2 | **内容生成层** | 团队建模框架（教育专家+建模大师+代码架构师）→ 流式生成 HTML | 结构化课程生成（JSON）+ 内容参数化（章节/题量/题型随难度与身份动态调整） |
| L3 | **质量保障层** | AI 审计 2 项指标（学术稳定性、趣味性）+ 反思评估 | **NEBULA 质量评估体系（详见 2.4）** |
| L4 | **交互交付层** | iframe 沙箱 + NDS 设计系统 + 8 语言 + 音效 + 红绿灯控制 | 确定性渲染器（AI 不再写前端代码）+ 移动端适配 |
| L5 | **数据沉淀层** | localStorage 历史 + Worker 日志 | 云端历史（D1）+ 学习画像 + 结构化日志 |
| L6 | **增长分发层** | 导出独立 HTML（离线分发） | 分享链接、课程库、B 端批量分发 |

### 2.4 NEBULA 质量评估体系（V2 核心设计）

现有系统只有"生成后 2 维打分"，V2 升级为**覆盖生成全生命周期的四段式评估体系**：

**① 生成前 · 适配预检（Pre-check）**
- 主题可解性分析：主题是否适合孵化课程（过于宽泛/敏感/无解 → 提示用户细化）。
- 身份适配分析：根据用户身份生成"内容侧重建议"（如外贸商 → 侧重实操清单）。

**② 生成中 · 结构校验（In-process）**
- 后端管线对生成内容逐项校验（章节完整性、题型合法性、字段非空、JSON Schema），失败自动重试/修复。
- 前端实时展示校验进度（"章节 3/5 · 题目 12/25 已就绪"）。

**③ 生成后 · 五维 AI 审计（Post-audit）**
- 从现有 2 维扩展到 **5 维雷达图**（0–100）：

| 维度 | 含义 |
|---|---|
| 学术准确性 Accuracy | 内容有无事实/公式错误 |
| 教学适配度 Fit | 与用户身份/目标/难度的匹配程度 |
| 交互深度 Depth | 引导质量、题型多样性、反馈颗粒度 |
| 趣味性 Fun | 剧情吸引力、游戏化设计 |
| 个性化指数 Personalization | 案例/情境与"这个人"的相关程度 |

- 综合分 + 五维雷达图可视化展示；低于阈值自动触发优化重生成。

**④ 学习后 · 反思评估 + 画像回流（Post-learning）**
- 学习者感悟 → AI 教授评分（0–100）+ 改进建议（保留"毒舌教授"风格，增加语气可选）。
- 学习者对课程打星反馈 → 与画像一起回流，持续优化 Prompt 与生成参数（数据飞轮）。

### 2.5 完整用户旅程

```
[登录门禁] → 输入学习主题 → AI 领域识别 + 适配预检
        → 填写身份/目标（如"外贸出口商，想寻找客户"）
        → 选择难度阶梯（入门/进阶/专家）+ 模型（V3 / R1）
        → 点击「开始建模生成」
        → 流式生成：结构校验进度实时可见 → 渲染器呈现完整课程
        → 五维 AI 质量审计（雷达图）
        → 用户在生成课程中学习：封面 → 章节剧情 → 每章测验
           （单选/多选/判断/连线/排序/案例分析，答错强制引导解析，
             答对才可进入下一题/下一章）
        → 「最终发现」环节：写下感悟 → AI 教授评分 + 建议 → 画像回流
        → 导出独立 HTML / 分享链接 / 查看云端历史 / AI Tutor 追问
```

### 2.6 设计思想（值得保留的精华）

1. **团队建模框架（Prompt 模拟多专家协同）**：把"教育专家、建模大师、代码架构师"的角色分工编码进生成 Prompt，一次生成即完成教学设计 + 内容撰写 + 前端实现。
2. **微观经济学激励设计**：得分、章节解锁、即时引导解析 → 降低学习门槛、提升即时回报。
3. **强制引导式学习（Scaffolding）**：认知支架式测验设计。
4. **多语言引擎（LinguaForce）**：中/英/法/俄/西/阿/日/德 8 种 UI 语言 + 任意自定义 ISO 语言的内容生成。
5. **Nebula Design System（NDS v4.0）**：完整设计令牌、Mac 深灰与纯白双主题——视觉完成度很高。

### 2.7 技术架构现状

```
┌─────────────────────────────────────────────────────┐
│  前端：单文件静态 HTML（111 KB，Cloudflare Pages）      │
│  ├─ NDS 设计系统（CSS 17 KB，设计令牌 + 组件层）        │
│  ├─ LinguaForce 多语言引擎（8 语言字典 + 内容语言指令）   │
│  ├─ 内联 JS 69 KB（生成器 / 流式解析 / 审计 / 历史）      │
│  └─ CDN：supabase-js@2（已加载但未使用）、marked.js     │
└──────────────────────────┬──────────────────────────┘
                           │ POST JSON { action: ... }
                           ▼
┌─────────────────────────────────────────────────────┐
│  后端：Cloudflare Worker（api.nebulavessel.com）      │
│  ├─ action=identify  领域识别（deepseek-chat）        │
│  ├─ action=generate  课程生成（流式 SSE）              │
│  ├─ action=audit     质量审计                          │
│  ├─ action=reflect   感悟评估                          │
│  └─ action=log       数据日志中转（→ 原 Supabase）      │
└──────────────────────────┬──────────────────────────┘
                           ▼
                    DeepSeek API（MoE 模型）
```

---

## 3. 目标用户

### 3.1 核心用户（C 端学习者）

**① 职业化定制学习者（第一核心）**
- 外贸/跨境电商从业者、程序员、产品经理、金融从业者等知识工作者。
- 痛点：通用课程与自己的职业场景脱节，学完不会用。
- NEBULA 解法：身份="外贸出口商，想寻找客户"→ 课程围绕获客渠道、实操清单、避坑指南展开，**学完即用**。

**② 多语种学习者（单独细分，V2 重点增长人群）**
- 三类典型：a) 用外语学习专业知识的人（如法语区学生学习经济学）；b) 外语学习者通过兴趣主题学语言（用目标语言生成任意主题课程，边学知识边学语言）；c) 非英语母语用户需要母语界面 + 母语化专业内容。
- 现有 8 语言 UI + 自定义 ISO + Content Only 模式是天然入口；V2 将针对该人群增强：RTL（阿拉伯语）排版适配、术语双语对照卡、发音/朗读支持（可选）。

**③ 终身学习者 / 兴趣探索者**
- 喜欢游戏化、剧情化学习体验的年轻人；对某领域产生好奇但不愿啃专业书的"轻学习者"。
- NEBULA 解法：入门级难度 + 类比 + 剧情，让冷知识变得好玩。

### 3.2 扩散用户（B 端，产品自然延伸）

**④ 教师 / 培训师 / 内容创作者（最易变现）**
- 一键把知识点变成可交互课件，导出独立 HTML 即可分发（学生打开即用，无需部署）。
- V2 增强：课程模板库、批量生成、品牌定制。

**⑤ 教育机构 / 企业内训**
- 为不同岗位定制"身份化"培训课程（如为外贸团队生成获客实操课），结合分享链接做培训管理。

**⑥ AI 产品观察者 / 开发者**
- NEBULA 本身是"AI 生成式前端 + 个性化教育"的示范案例。

### 3.3 用户画像示例

| 画像 | 场景 | 痛点 | NEBULA 解法 |
|---|---|---|---|
| 跨境电商新手 | 想学 Google Ads 投放 | 网课太泛，不知如何起步 | 身份="想做跨境投放的新手"，生成剧情式实操课 |
| 考研学生 | 拉格朗日乘子法不理解 | 课本公式枯燥 | 专家级 + 公式推导 + 情境类比 + 引导测验 |
| 法语学习者 | 学经济学专业术语 | 找不到母语化专业材料 | Content Only 模式：法语界面生成目标语言内容 |
| 企业内训师 | 为新员工做产品培训 | 做课件耗时 | 一键生成 + 导出 + 分享链接分发 |
| 外贸业务员 | 想开拓东南亚市场 | 课程与业务脱节 | 身份化课程：市场分析框架 + 客户开发实操 |

---

## 4. 核心功能清单（现状）

| 模块 | 功能 | 状态 |
|---|---|---|
| 接入门禁 | 密码登录遮罩（admin/test，前端明文校验） | ⚠️ 安全脆弱 |
| 生成引擎 | 主题 + 身份 + 难度 → 流式生成完整 HTML 课程（SPA：封面/5 章节/每章 5 题） | ✅ |
| 领域识别 | AI 自动归类学术领域（多语言输出） | ✅ |
| 多语言 | 8 种 UI 语言 + 自定义 ISO + Content Only 内容语言模式 | ✅ |
| 质量审计 | 生成后 AI 对"学术稳定性/教学趣味性"打分（0–100） | ⚠️ 仅 2 维，JSON 解析无容错 |
| 反思评估 | 学习感悟 → AI 教授评分 + 建议（毒舌风格） | ✅ |
| 预览终端 | iframe 沙箱 + 红绿灯（清空/恢复、折叠侧栏、全屏） | ✅ |
| 历史记录 | 本地历史列表（localStorage，可点击回放） | ⚠️ 仅本地 |
| 导出 | 导出独立 HTML 文件 | ⚠️ 需修复验证 |
| 数据日志 | 生成记录（主题/难度/源码/推理过程/领域/身份）→ Worker → 数据库 | ⚠️ 无鉴权 |
| 设置 | 深/浅主题、语言持久化 | ✅ |
| 音效 | Web Audio 登录/成功/错误提示音 | ✅ |

---

## 5. 现状问题诊断（本次改造的 Bug 清单）

### A 级：功能失效 / 安全（必须修）
1. **模型选择形同虚设**：前端可选 DeepSeek-V3 / R1，但实测请求 `deepseek-chat` 返回的是 `deepseek-v4-flash`——后端 Worker 覆盖了模型参数，用户选择不生效。
2. **重试机制从未实现**：`ENTERPRISE_CONFIG` 定义了 `TIMEOUT: 60000 / MAX_RETRIES: 3`，但全代码只有定义处引用——超时与失败重试根本没接上。
3. **登录密码明文硬编码**（`admin`/`test`）在前端源码中，任何人查看源码即可进入；后端 `action=log` 等接口无任何鉴权，可被任意滥用刷数据。
4. **Supabase SDK 加载但从未使用**（浪费约 90 KB 加载），历史记录仅存 localStorage——换设备/清缓存即丢失，且 `clearHistory` 只清 DOM 不清存储，刷新后"已清空"的历史会复活。
5. **导出功能需修复/验证**：`exportHTML` 是 `CoreAgent` 的对象方法，但按钮以全局函数方式调用，存在引用错误风险。

### B 级：AI 生成稳定性（本次核心课题）
6. **Prompt 指令矛盾堆叠（最大稳定性杀手）**：`promptAddons` 中"严禁 onclick、必须 addEventListener"与"MANDATORY OVERRIDES 必须 window.xxx + onclick"直接冲突；"修改一~九"等十余批历史补丁层层叠加，指令互相打架——模型无所适从，生成质量随 prompt 增长而劣化。
7. **无 HTML 完整性校验**：不检查标签闭合、DOCTYPE、结构完整度，截断的 HTML 直接进 iframe。
8. **无 JS 语法校验**：生成代码中的 JS 错误被注入的 `window.onerror = () => true` 静默吞掉——用户看不到报错，但页面交互全部失灵，且无法定位问题。
9. **无 Markdown 包裹剥离兜底**：只靠 prompt 口头要求"不要 Markdown 标记"，一旦模型输出 ```html 代码块，整个页面渲染即失败。
10. **长内容截断无续写**：单次 `max_tokens: 8192` 生成完整课程（5 章 × 5 题 + 样式 + 逻辑）极易中途截断，截断后无检测、无续写、无降级。
11. **审计 JSON 解析无容错**：`JSON.parse` 模型输出失败时指标直接显示 `Err`，体验断裂。
12. **流式解析脆弱**：只处理 `data: ` 前缀行，对 Worker 返回格式变化零容忍。

### C 级：工程与维护
13. 单文件 111 KB：CSS/JS/HTML 全内联，模块注释重复（如 System Logic 出现两次），难以维护、测试与多人协作。
14. 移动端禁用缩放（`user-scalable=no`），小屏体验未验证。
15. 日志数据无结构校验、无上限控制，长期运行数据库会膨胀。

---

## 6. V2 改造蓝图（PRD 确认后的实施方案）

> 目标：**修掉全部 A/B 级问题，将静态单文件升级为「前后端分离的动态网页」，并用"结构化生成 + 确定性渲染"架构根治 AI 生成内容不稳定性；同时按评审意见升级为六层系统 + 四段式评估体系 + 参数化内容设计。**

### 6.1 新架构总览

```
┌──────────────────────────────────────────────────────────┐
│ 前端（Vite 工程，静态资源 + 动态数据）                       │
│  ├─ 模块化：design-system / i18n / api-client /            │
│  │   generator / validator / renderer / history / auth     │
│  ├─ 确定性课程渲染器（模板化：封面/章节/题型/引导/评分）      │
│  │   —— AI 不再生成 HTML，只生成结构化 JSON 内容             │
│  └─ 评估看板：五维雷达图 / 校验进度 / 反思反馈               │
└───────────────┬──────────────────────────────────────────┘
                │ POST /api/*（鉴权 Token）
                ▼
┌──────────────────────────────────────────────────────────┐
│ 后端（Cloudflare Worker + D1/KV）                          │
│  ├─ POST /api/generate   结构化课程生成（流式/非流式）        │
│  ├─ POST /api/audit      五维质量审计                       │
│  ├─ POST /api/reflect    感悟评估 + 画像回流                 │
│  ├─ POST /api/identify   领域识别 + 适配预检                 │
│  ├─ GET/POST /api/history  云端历史（按用户）                │
│  ├─ POST /api/share      生成分享链接                       │
│  ├─ POST /api/auth       登录/登出（服务端校验）              │
│  ├─ 生成管线：Schema 校验 → 重试 → 修复 → 降级               │
│  └─ 模型路由：V3 / R1 真实生效（失败自动降级）               │
└───────────────┬──────────────────────────────────────────┘
                ▼
        DeepSeek API（deepseek-chat / deepseek-reasoner）
```

### 6.2 稳定性核心方案：从「生成 HTML」到「生成 JSON + 模板渲染」

这是根治 AI 生成不稳定的关键架构决策：

1. **AI 只输出结构化 JSON**（章节、题目、选项、解析、引导文案），通过 JSON Schema 约束 + 后端解析校验。JSON 可比 HTML 稳定一个数量级。
2. **前端确定性渲染器**把 JSON 渲染成课程页面——HTML/CSS/JS 全部由我们手写维护，**不存在 AI 写坏的代码**，交互 100% 可靠。
3. **后端生成管线兜底**（Worker 侧）：
   - ① Schema 校验（字段完整性、题型合法性）→ 失败自动重试（最多 3 次，带错误诊断提示）
   - ② 轻量修复（剥离 markdown 围栏、截断 JSON 续写、字段补默认值）
   - ③ 最终降级：返回内置模板课程（保证任何情况下用户都能得到可用结果）
4. **Prompt 重构**：清除全部历史补丁与矛盾指令，重建单一、清晰、无冲突的系统提示词（含 few-shot 示例），并按难度/语言参数化。
5. **流式体验保留**：生成过程仍可流式展示（"教育专家团队"进度文案），但流的是 JSON 片段 + 渲染器实时预览，而非裸 HTML。
6. **审计与反思改为结构化输出**（JSON Schema / 正则兜底），杜绝 `Err`。
7. **保留导出功能**：导出时把「JSON 内容 + 渲染器 + 样式」打包为独立 HTML——依然可离线分发。

### 6.3 内容设计参数化（响应评审：题量可调）

课程结构不再硬编码"5 章 × 5 题"，改为**随难度、身份与主题动态配置**：

| 难度 | 章节数 | 每章题量 | 题型侧重 | 内容风格 |
|---|---|---|---|---|
| 入门级 | 3 章 | 3 题 | 单选 + 判断为主，穿插 1 个趣味互动 | 短篇幅、类比多、剧情轻松 |
| 进阶级 | 4 章 | 4–5 题 | 单选/多选/连线均衡 | 理论 + 应用结合 |
| 专家级 | 5 章 | 5 题 | 含排序/案例分析/开放推导 | 公式推导、底层逻辑、争议研究 |

- 生成前由 AI 根据主题复杂度建议章节数（用户可微调），或直接按难度模板取默认值。
- 每题必带"错误引导解析"（认知支架）；题型分布由后端参数控制，保证多样性。
- 未来可开放"课程时长"档位（10 分钟快餐课 / 45 分钟深度课），进一步个性化。

### 6.4 动态化方案

| 能力 | 现状 | V2 |
|---|---|---|
| 登录 | 前端明文密码 | Worker 服务端校验（Token 会话，防查看源码绕过） |
| 历史记录 | localStorage | 云端存储（D1），多设备同步，真删除 |
| 数据日志 | 无鉴权转发 | 带 Token + 限流 + 结构化校验 |
| 模型选择 | 失效 | 后端真实路由 V3/R1（含失败自动降级到备选模型） |
| 配置 | 硬编码前端 | 后端环境变量（API Key、模型、开关） |
| 分享 | 无 | 生成的课程可通过唯一 ID 生成可分享链接（动态网页的核心增值能力） |
| 评估 | 2 维分数 | 五维雷达图 + 画像回流 |

### 6.5 分阶段里程碑

- **M1 修复 + 稳定（先保当前体验）**：修 A 级 bug（登录鉴权、模型路由、重试机制、导出、历史清理）；加 markdown 剥离 + HTML/JS 校验 + 截断检测 + 审计容错。前端拆分模块化（不改变现有交互）。
- **M2 架构升级（治本）**：JSON 结构化生成 + 确定性渲染器 + Worker 生成管线（Schema 校验/重试/修复/降级），Prompt 全面重构，内容参数化。
- **M3 动态化 + 评估体系（增值）**：云端历史/日志、Token 鉴权、分享链接、五维审计雷达图、画像回流。
- **M4 体验打磨（可选）**：移动端响应式、多语种学习者增强（RTL 排版、术语双语卡）、AI Tutor 增强、B 端分发模板。

### 6.6 兼容与风险

- 现有用户生成的历史 HTML（localStorage）在 M1 阶段保留兼容读取；M2 后旧格式仅可回放不可再生成。
- 后端 Worker 改造需保持 `api.nebulavessel.com` 的旧接口兼容过渡期，避免线上中断（本机开发环境先行验证）。
- 渲染器需覆盖现有全部题型（单选/多选/判断/连线/排序/案例分析）与引导弹窗、音效、postMessage 回传，做到体验不降级。

---

## 7. 验收标准（V2 完成后）

1. 生成成功率 ≥ 98%（含降级兜底），连续生成 20 次无白屏/无交互失灵。
2. 生成的课程中不存在 AI 编写的 HTML/JS——全部由渲染器输出，前端交互错误率归零。
3. 模型选择真实生效：V3 与 R1 请求在后端日志中可区分。
4. 登录无法通过查看前端源码绕过；API 无 Token 请求被拒绝。
5. 历史记录跨设备同步；清空后刷新不复活。
6. 超时/失败自动重试（≤3 次），重试仍失败时用户得到可用的降级课程而非报错。
7. 导出文件双击即可离线运行，与在线版体验一致。
8. 同一主题在 3 种难度/3 种身份下生成结构显著不同（个性化可验证）；题量随难度档位变化。
9. 五维审计雷达图正常展示；反思评估返回稳定 JSON。

---

## 8. V2/V3 实施结果与新增能力（已交付）

> 按本 PRD 第 6 节蓝图 + 后续评审扩展，以下能力已全部实现并通过自动化测试（API 冒烟 43/43 通过、浏览器 E2E 25/25 通过）。

### 8.1 稳定性架构（根治 AI 生成不稳定）

1. **Function Calling 硬约束**：大纲/讲解/题卡全部通过 API 级 JSON Schema（`tools` + `tool_choice: required`）强制结构化输出，替代提示词手写 JSON 格式说明；自定义模型三级降级（function calling → json_object → 纯文本 + extractJson 兜底）。
2. **题卡级分解**：每道题独立生成（输出 300~600 token，JSON 损坏概率大幅降低），独立校验/重试/兜底，单题失败不影响整门课。
3. **三段式生成管线**：大纲（小 JSON）→ 各章讲解并行 → 题卡并行（间隔重复自动关联前章要点）→ 组装校验 → 五维审计；任何环节失败均有内置兜底课程/题卡（**永远可交付**）。
4. **生成参数**：`temperature 0.2 / max_tokens 分层 / 90s 超时 / 指数退避重试 / Pro 失败自动降级 Flash`。

### 8.2 教育学理论融合（生成逻辑与产品结构）

NEBULA 的设计锚定学习科学实证，已系统化落地：

| 理论 | 出处 | 落地方式 |
|---|---|---|
| 提取练习（最高效用） | Dunlosky et al. 2013 元分析；Roediger & Karpicke 2006（61% vs 40%） | 每章预热问题（先想后学）、测验、每日一题、错题复习 |
| 间隔重复 | Cepeda et al. 2006（约 2× 保持） | 每章"温故知新"自动回顾前章要点、每日一题 |
| 最近发展区 ZPD | Vygotsky 1978 | 难度阶梯 × 水平画像，内容定在"当前水平+1" |
| 生成效应 | Slamecka & Graf 1978 | 案例/填空题、终章"费曼输出"（讲给朋友听） |
| 主动学习 | Freeman et al. 2014 PNAS（失败率 -1/3） | 整个课程即主动学习（章节解锁/交互测验/反思） |
| 认知负荷 | Sweller 1988 | 每段一个概念、术语首现必解释 |
| 自我决定理论 | Deci & Ryan 2000 | 自主=身份化定制；胜任=XP/反馈；归属=社区 |
| Octalysis 游戏化 | Yu-kai Chou 2015 | 学习路径/XP/徽章/排行榜/每日一题/连击 |
| 成长型思维 | Dweck 2006 | 错误引导文案传递"错误是路标" |
| 心流 | Csikszentmihalyi 1990 | 难度渐进、章节钩子、即时反馈 |
| 布鲁姆认知层级 | Bloom 1956 | 题目覆盖记忆→理解→应用→分析四层+ |

### 8.3 新增产品能力

- **用户体系**：注册/登录（PBKDF2 加盐哈希、JWT 会话）、归属学校、管理员角色（ADMIN_EMAILS）、访问令牌模式兼容。
- **个人主页**：学习统计、XP/等级（Lv1-10）、连续学习 Streak、8 类成就徽章、错题本、我的课程/帖子。
- **学习社区**：发布课程（**AI 审核 → 未通过进管理员人工复核队列**）、评论（AI 审核）、点赞、克隆他人课程到我的孵化台、搜索/热门/最新、排行榜（学习榜/贡献榜/人气榜）。
- **内容管理后台**：人工复核队列（AI 拒绝原因 + 课程预览 + 通过/驳回）、帖子管理（删除）。
- **激励系统**：XP 积分（生成/完成/发布/评论/获赞/每日一题/复习课）、每日打卡 Streak、成就徽章。
- **每日一题**：按用户最近学习主题生成趣味题（按天缓存），答对 +20 XP。
- **AI 学习路径**：大目标 → 4~6 门课程序列，一键开始任意课程。
- **错题强化**：课程内错题自动收集 → 个人主页错题本 → 一键生成"针对性复习课"。
- **学习闭环**：AI 教授点评附"下一步学习建议"，可一键生成新课程。
- **模型系统**：预设 DeepSeek-V4-Flash0731 / DeepSeek-V4-Pro0813（可在 wrangler.toml 配置实际模型名）+ **自定义模型接入（BYOK）**：任意 OpenAI 兼容 API（Base URL + Key + 模型名），配置仅存浏览器、Key 不落服务器，三级结构化降级保证兼容。
- **多语言**：UI 12 种语言（中/英/法/俄/西/阿/日/德/葡/意/韩/印地语）+ 任意 ISO 内容语言（含 RTL 支持）。
- **介绍展示页（About）**：定位、四大理念、10 张理论卡片（含出处）、关键数据（-35% 失败率、2×、50%+、90%）、六层闭环、可信与安全说明、参考文献。
- **星云动画 UI**：星点漂浮、星云漂移、飞船悬浮光晕、渐变流动按钮、页面过渡、徽章点亮、连击反馈、尊重 reduced-motion。
- **课程体验**：预热卡/回顾卡/费曼输出/连击提示、错题自动收集、五维雷达图、分享链接、独立 HTML 导出（离线可用）。

### 8.4 工程交付

- monorepo：`web/`（Vite 前端，20 模块）+ `worker/`（Cloudflare Worker，15 模块）+ `scripts/`（冒烟 + E2E 测试）。
- 自动化测试：`node scripts/smoke-api.mjs`（43 项 API 断言）、`node scripts/e2e.mjs`（25 项浏览器断言）。
- 部署目标：前端 → www.nebulavessel.com（Cloudflare Pages）；后端 → api.nebulavessel.com（Cloudflare Worker + D1）；详见 README.md。

---

## 8.5 V3.1 实施记录（2026-08 第二批）

> 23 项评审需求已全部落地并部署上线（除 Google 登录等待 OAuth 凭据）。

| 类别 | 交付 |
|---|---|
| 注册/登录 | 邮箱验证码（Resend，发件人 noreply@nebulavessel.com）、Cloudflare Turnstile 人机验证、密码逐字符匹配动画+小眼睛、Google 登录（代码就绪，配置 GOOGLE_CLIENT_ID/SECRET 即激活） |
| 学校 | 内置中国百强高中 100 所 + 各地区知名初中 66 所下拉选择；未收录学校可申请，管理员审核通过后入库 |
| 合规 | ToS / Privacy Policy / 跨境数据传输确认三份文档（中英双语，GDPR 标准结构），注册强制勾选 |
| 个人主页 | 头像上传（canvas 压缩）、个人介绍、研究领域、微信/电话/邮箱联系方式；他人主页（社区点击作者查看）；每日抽签（洛谷风格 24 签：宜/忌/学习箴言/幸运值，每人每天一签可换签）；移除等级 Lv |
| 联系我们 | GET IN TOUCH 页面：官方邮箱/微信/地区 + 合作区块 + 工单表单 → 后台查看/标记答复 |
| AI 助教 | 侧边栏聊天面板：课程绑定对话（生成即自动开对话）、强制主题相关（偏离自动拉回）、DuckDuckGo 联网搜索（默认开启）、Flash/Pro/自定义模型、流式回复、历史保留、引导前沿研究与跨学科 |
| 内容管理后台 | 四个页签：人工复核队列 / 帖子管理 / 学校审核 / 联系工单 |
| 体验 | 五维审计说明（可折叠）、章节数 2~10、输入框占位文案优化、设置精简（改密码/删 ISO/去 Gemini）、关于 NEBULA 导航置顶并设为默认首页、每日一题每天最多 3 次刷新、文献更新（经典+近年：Agarwal 2021、Yeager 2019 Nature 等） |
| 图标 | 全站 emoji 替换为 Lucide 风格内联 SVG 线条图标（本地无外网依赖）；koboyo.com 经核实为画布工具而非图标库，故采用同风格开源的 Lucide 体系自绘 |
| 文档 | COMMUNITY_MODERATION.md（AI 审核机制专档） |
| 测试 | API 冒烟 43/43 + E2E 33/33 全绿 |

### 激活 Google 登录（唯一待用户操作）

1. Google Cloud Console → 创建 OAuth 客户端（Web）→ Authorized redirect URIs 填 `https://api.nebulavessel.com/api/auth/google/callback`
2. 把 Client ID 发我（写入 wrangler.toml vars），Secret 用 `wrangler secret put GOOGLE_CLIENT_SECRET` 设置
3. 登录页即出现「使用 Google 登录」按钮

## 8.6 V3.2 实施记录（管理员用户管理，2026-08）

- 后台新增第五个页签「用户管理」：全量用户列表（姓名/邮箱/学校/角色/课数/帖数/获赞/注册时间），支持姓名、邮箱、学校关键字搜索（`GET /api/admin/users?q=`，仅 admin，非管理员 403）。
- 每行「查看主页」按钮跳转该用户公开主页 `#/user/<id>`；ADMIN 徽标高亮管理员账号。
- 验证方式：本地签发管理员 JWT（HS256，与线上 `issueSession` 同构）→ 接口返回 3 个真实用户、`q=sam` 命中 Sam Su；Playwright 生产 E2E（`scripts/admin-users-check.mjs`）5/5 全绿（Tab 存在/列表渲染/搜索命中/主页跳转/无页面错误）。
- 附注：ADMIN_EMAILS 环境变量（sujunyue_sam@outlook.com）在注册与登录时动态升级角色；测试用临时管理员账号验证后已从 D1 删除。
- 管理员用户管理增强：新增「删除用户」（`DELETE /api/admin/users/<id>`），级联清理课程/帖子/评论/点赞/主页点赞/通知/对话/活动/错题/事件/工单/验证码后删除账号；保护规则：不能删除自己、不能删除管理员、不能删除系统访客账号（nebula-user）。前端确认弹窗 + 12 语言词条；Playwright 生产验证 5/5（`scripts/admin-delete-user-check.mjs`）。
- 评论 AI 审核升级：评论被 AI 判定不通过时不再直接隐藏，而是进入人工复核队列（status=pending，`ai_review` 记录理由）；管理员在复核队列中按卡片审核（通过 → 公开可见 / 驳回需理由），结果通知评论作者（comment_approved / comment_rejected）；帖子与评论混合按时间排序展示。D1：comments 表新增 ai_review/reject_reason 列。
- 专属 emoji 全覆盖：注册未选 emoji 时按邮箱哈希自动分配预设 emoji；老用户与 Google 登录用户在首次登录时自动补齐；JWT payload 携带 emoji，左下角 rail 始终显示「专属 emoji + 昵称」（emoji 优先于头像，未设置时兜底 🧑‍🚀）。
## 8.7 V3.3 实施记录（质量修复 + 微信登录 + 双语精简，2026-08）

- **题目占位 bug 根因修复**：历史课程测验题几乎全是兜底模板（"下列哪项最接近「本章核心概念」…"）。根因有二：① `quizCardFormatHint` 的 JSON 模板缺少 `type` 字段，模型严格照模板输出 → 校验「题型非法或缺失」→ 全部落兜底；② multi/order 长题输出超 900 token 被截断 → JSON 解析失败。修复：模板加 type + 管线按请求题型兜底补齐 + maxTokens 900→1600 + 提示词要求选项/引导精炼。实测 6/6 题全部真实跟随主题（电磁感应/开普勒定律）。
- **按反馈重新生成**：课程完成页新增「想调整这门课吗？」（与分享社区并列），用户输入意见 → `POST /api/regenerate` 沿用原课程全部画像参数 + 反馈注入大纲/讲解/题目三层提示词 → SSE 重新生成并替换。实测反馈"换成高考难度应用题"生效（生成近日点距离计算题）。权限：只能重新生成自己的课程。
- **六维 AI 审计**：新增 compliance（需求一致性：章节数、题量与内容范围是否与要求一致），雷达图五维→六维，评估说明同步更新。
- **生成预计用时**：生成进度条下新增「⏱ 预计用时」——Flash 约 1~2 分钟、Pro 约 3~5 分钟、自定义模型取决于服务速度（随模型选择显示）。
- **聊天滚动修复**：流式回复结束后重建 DOM 导致消息框跳回顶部 → 重建后强制滚到底；delta 期间改为「接近底部才跟随」，不再打断翻阅历史。
- **全站仅保留中英双语**：删除 fr/ru/es/ar/ja/de/pt/it/ko/hi 全部 UI 语言库与选择项（linguaforce/extra/about/contact/legal/课程运行时词条，共清理约 74KB，主包 228KB→146KB）；旧 localStorage 语言自动回退中文。
- **微信登录（代码就绪）**：`/api/auth/wechat` + callback 实现微信开放平台「网站应用」扫码登录（code 换 token → openid 派生内部邮箱 → 自动注册/登录/分配 emoji → 签发 JWT）；登录页绿色「使用微信登录」按钮按 config.wechatEnabled 显隐。**待用户侧**：微信开放平台企业认证 + 创建网站应用（回调域 https://api.nebulavessel.com，需备案域名）→ 提供 AppID 写入 vars、AppSecret 用 `wrangler secret put WECHAT_APP_SECRET`。
- 修复：学校申请无需登录——`/api/schools/request` 移出会话守卫（注册时列表找不到学校即可直接申请），已登录则自动记录申请人，未登录申请 applicant_id 为 null；新增 IP 限频（5 次/小时）防滥用；防重复提交（已在审核中拦截）保留。浏览器实测注册页未登录提交成功（`scripts/school-request-check.mjs` 2/2）。

---

## 9. 附录：关键技术事实（源码分析记录）

- 线上版本号：`15.1.0-Fix`（meta version），前端 111,550 字节单 HTML；内联 CSS 16,772 字符；内联 JS 68,947 字符。
- 后端 API：`https://api.nebulavessel.com`（Cloudflare Worker），action 枚举：`identify / generate / audit / reflect / log`；另有历史遗留地址 `nebulaaiagent.sujunyue-sam.workers.dev`。
- 生成参数：`max_tokens: 8192, temperature: 0.1, top_p: 0.85, stream: true`（SSE，解析 `data:` 行，取 `choices[0].delta.content`，另收集 `reasoning_content`）。
- 实测：POST 请求 `model: deepseek-chat` 返回 `model: deepseek-v4-flash`（后端覆盖模型参数）。
- 生成后注入 `universalFix` 脚本：兜底翻页逻辑、AI Tutor 聊天绑定、`window.onerror` 静默吞错。
- 登录密码：`admin` / `test`（前端明文 if 判断）；设置持久化：`localStorage`（`nebula_lang` / `nebula_theme` / `nebula_custom_iso`）。
- CDN 依赖：`@supabase/supabase-js@2`（未使用）、`marked.min.js`（聊天 Markdown 渲染）。
